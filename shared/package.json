{
  "name": "@ai-menu/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for ai-menu project",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}

