# 🌐 域名解析配置指南 - ai-menu.tech

## 📍 第一步：找到域名在哪里管理

### 问题：阿里云DNS显示"没有数据"的原因

从您的截图看，阿里云DNS的"公网权威解析"页面是空的。这意味着：

**ai-menu.tech 这个域名不是在阿里云解析的！**

### 🔍 查找域名注册商

运行以下命令查看域名在哪里注册：

```bash
whois ai-menu.tech
```

或者访问：https://www.whois.com/whois/ai-menu.tech

**常见情况：**
- 如果在 **Namecheap**：登录 namecheap.com → Domain List → ai-menu.tech → Advanced DNS
- 如果在 **GoDaddy**：登录 godaddy.com → My Products → Domains → DNS
- 如果在 **Cloudflare**：登录 dash.cloudflare.com → ai-menu.tech → DNS
- 如果在 **阿里云万网**：阿里云控制台 → 域名 → 域名列表

---

## 📋 第二步：配置DNS解析记录

### 方案A：在原域名注册商配置（推荐）

找到域名管理页面后，添加以下记录：

| 类型 | 主机记录 | 记录值 | TTL |
|------|---------|--------|-----|
| A | api | 8.140.9.139 | 600 |
| CNAME | app | cname.vercel-dns.com | 600 |
| CNAME | admin | cname.vercel-dns.com | 600 |

**详细说明：**

```
api.ai-menu.tech  → A记录 → 8.140.9.139
  ↑ 指向您的阿里云ECS服务器

app.ai-menu.tech  → CNAME → cname.vercel-dns.com  
  ↑ 前端用户页面（Vercel托管）

admin.ai-menu.tech → CNAME → cname.vercel-dns.com
  ↑ 管理后台（Vercel托管）
```

### 方案B：转移DNS到阿里云（可选）

如果您想统一在阿里云管理，需要：

1. **在阿里云DNS添加域名**
   - 进入：阿里云控制台 → 云解析DNS → 添加域名
   - 输入：ai-menu.tech
   - 点击"确定"

2. **阿里云会给您DNS服务器地址**，例如：
   ```
   ns1.alidns.com
   ns2.alidns.com
   ```

3. **在原域名注册商修改Name Servers**
   - 登录域名注册商（如Namecheap）
   - 找到"Nameservers"设置
   - 改为阿里云的DNS服务器
   - 等待24-48小时生效

⚠️ **注意**：修改NS记录会影响所有子域名，请谨慎操作！

---

## 🔍 第三步：验证DNS配置

### 检查DNS是否生效

```bash
# 检查A记录
nslookup api.ai-menu.tech

# 应该返回：
# Name: api.ai-menu.tech
# Address: 8.140.9.139

# 检查CNAME记录
nslookup app.ai-menu.tech
nslookup admin.ai-menu.tech
```

### 或者使用在线工具

访问：https://dnschecker.org/
- 输入域名：api.ai-menu.tech
- 选择记录类型：A
- 查看全球解析状态

---

## 📸 各平台DNS配置截图指南

### Namecheap（最常见）

1. 登录 namecheap.com
2. 点击右上角头像 → Domain List
3. 找到 ai-menu.tech → 点击 "Manage"
4. 点击 "Advanced DNS" 标签
5. 点击 "Add New Record" 添加以下记录：

```
Type: A Record
Host: api
Value: 8.140.9.139
TTL: Automatic

Type: CNAME Record
Host: app
Value: cname.vercel-dns.com
TTL: Automatic

Type: CNAME Record
Host: admin
Value: cname.vercel-dns.com
TTL: Automatic
```

### Cloudflare

1. 登录 dash.cloudflare.com
2. 选择 ai-menu.tech 域名
3. 点击左侧 "DNS" 菜单
4. 点击 "Add record" 添加：

```
Type: A
Name: api
IPv4 address: 8.140.9.139
Proxy status: DNS only (灰色云朵)
TTL: Auto

Type: CNAME
Name: app
Target: cname.vercel-dns.com
Proxy status: DNS only
TTL: Auto

Type: CNAME
Name: admin
Target: cname.vercel-dns.com
Proxy status: DNS only
TTL: Auto
```

⚠️ **重要**：Cloudflare的Proxy一定要关闭（显示灰色云朵），否则Vercel的SSL证书会有问题！

### 阿里云万网

如果域名在阿里云买的：

1. 阿里云控制台 → 域名
2. 找到 ai-menu.tech → 点击"解析"
3. 点击"添加记录"：

```
记录类型: A
主机记录: api
记录值: 8.140.9.139
TTL: 10分钟

记录类型: CNAME
主机记录: app
记录值: cname.vercel-dns.com
TTL: 10分钟

记录类型: CNAME
主机记录: admin
记录值: cname.vercel-dns.com
TTL: 10分钟
```

---

## ⏰ DNS生效时间

- **新增记录**：5-10分钟（大部分情况）
- **修改记录**：10-30分钟
- **修改NS服务器**：24-48小时

---

## 🆘 如果实在找不到域名在哪里

请提供以下信息给我：

1. 运行命令：`whois ai-menu.tech` 的结果
2. 或者告诉我域名是在哪里购买的
3. 我可以帮您精确定位DNS配置位置

---

## ✅ 配置完成后的检查清单

- [ ] DNS记录已添加（api/app/admin）
- [ ] 使用 nslookup 验证解析正确
- [ ] 等待5-10分钟DNS生效
- [ ] 可以ping通 api.ai-menu.tech
- [ ] 准备配置HTTPS证书（下一步）

